# My Preferred Organization: Selector order

- "sweeping" CSS first
  - `*` selectors, `body`, `html`
  - "resets"
  - Any common defaults (Ex: `p`, etc)
- General Page order for next selectors
  - Often BEM style (coming!)
  - Try to select based on class

