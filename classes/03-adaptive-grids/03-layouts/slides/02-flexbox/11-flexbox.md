# Let's use flexbox

- `display: flex;` **arranges child elements**
- `<ul>` is parent of `<li>` elements

```css
ul {
    display: flex;

    /* other css here */
}
```

Now we have a (squished) row!
