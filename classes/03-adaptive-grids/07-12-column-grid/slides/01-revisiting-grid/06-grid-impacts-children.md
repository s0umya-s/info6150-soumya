# display: grid impacts direct children

Remember how `display: grid;` works
- Only affects direct children
- To match non-child elements to grid
  - Make wrapper width 100%
  - Recreate the same grid inside wrapper

